# Scoped lint config for the host test tree (inherits the root CPPLINT.cfg).
# readability/check assumes gtest-style CHECK_EQ(a, b); doctest's idiomatic
# form is CHECK(a == b) — its expression decomposition prints both operands
# on failure, which CHECK_EQ-style macros exist to work around.
filter=-readability/check
