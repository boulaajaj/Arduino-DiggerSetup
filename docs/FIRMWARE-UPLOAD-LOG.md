# Firmware Upload Log

Every firmware upload to the digger gets a row here: date, version, commit
SHA, branch, board/port, and notes on what was tested.

| Date | Version | SHA | Branch | Board / Port | Notes |
| --- | --- | --- | --- | --- | --- |
| 2026-05-31 | rc_test V7.6 | af9f84c | claude/v8-uno-r4-wifi-migration | UNO R4 WiFi / COM7 | First UNO R4 WiFi bring-up. Compiles 22% flash / 25% RAM. S.BUS verified live on SCI0/D12 after fixing inverter wiring (collector↔base swap). Throttle, steering, curvatureDrive differential on D9/D10, and Eco reverse cap (1375 µs) all confirmed. FS=0/Lost=0. X.BUS telemetry not polled by this firmware. |
| 2026-06-11 | rc_test V7.7 | 181f4eb | claude/restore-xbus-telemetry-36 | UNO R4 WiFi / COM7 | Added non-blocking X.BUS Read Register (0x10) telemetry on Serial1/D0-D1 — telemetry WITH control (0x10 never enters BUS_MODE). 58.9 KB, 22% flash / 26% RAM. Confirmed live: ESC0 reads 11.9V / ESC 28°C / motor 22°C / RPM tracks throttle (7→38 Hz); control intact (FS=0/Lost=0, outputs follow stick). ESC1 silent (OK1=0) — wiring or X.BUS address. Bus current 0.0A at idle (verify under load). CSV format changed (added per-ESC telemetry columns). |
| 2026-06-13 | rc_test V7.8 | 8f92d38 | claude/restore-xbus-telemetry-36 | UNO R4 WiFi / COM7 | Wi-Fi AP telemetry server (WiFiS3) + live dashboard. AP "Digger-Telemetry" (pass digger12345) @ 192.168.4.1; GET /data → telemetry JSON (CORS *), monitoring only. beginAP status 7 (AP_LISTENING) confirmed on boot; both ESCs OK while AP up. Non-blocking server (1 client/loop, 20ms bounded read). dashboard/index.html rewired sim→live /data polling (5Hz) + NO-LINK watchdog. 69.8 KB, 26% flash / 28% RAM. Includes wifiDebug() 3s status print (bring-up; remove later). |
| 2026-06-13 | rc_test V7.8 | 5253350 | claude/restore-xbus-telemetry-36 | UNO R4 WiFi / COM7 | Embed dashboard in firmware (web_page.h, PROGMEM) and serve at "/". iPhone confirmed reaching 192.168.4.1 and loading the full dashboard. 36% flash / 28% RAM. |
| 2026-06-13 | rc_test V7.8 | 1ae930e | claude/restore-xbus-telemetry-36 | UNO R4 WiFi / COM7 | Perf: HTTP-poll → SSE streaming (GET /events, 10Hz push, EventSource auto-reconnect) — fixes ~1-2Hz update rate from per-poll TCP setup. X.BUS poll gap 80→10ms / timeout 12→6ms (flaky ESC barely stalls healthy one). Removed Google Fonts @import (offline AP stalled first paint). 36% flash / 29% RAM. Field note: breadboard X.BUS drops one ESC under vibration — solder board (#43) to fix. |
| 2026-06-14 | rc_test V7.8 | 0875a87 | claude/restore-xbus-telemetry-36 | UNO R4 WiFi / COM7 | JOY_STEER_DIR = -1.0f flips joystick X polarity (right stick → right turn) after operator rewired the joystick Y cable on the soldered board. RC steering untouched. Field-verified by operator. 36% flash / 29% RAM. Interface board solder build now in service: Left ESC BEC (7.4V) → Arduino barrel jack works standalone; X.BUS telemetry both ESCs OK; S.BUS inverter works with R3 = 1k (vs documented 10k). |
| 2026-06-20 | rc_test V7.9 | dabfbf1 | claude/wifi-latency-54 | UNO R4 WiFi / COM7 | Wi-Fi serving-latency mitigation (#54), [WIFI] section only — no control-path change. AP pinned to channel 11; SSE dashboard push 10→5 Hz (X.BUS poll rate unchanged); 3 per-frame SSE writes coalesced into one buffer + single write() (one AT round-trip vs three); modem.timeout(50) around wifiUpdate() bounds worst-case loop stall at 50 ms vs WiFiS3 default 10 s. 39% flash / 29% RAM. Compiled + uploaded clean. Field test pending: target input-to-track latency < 300 ms with phone connected, FS=0/Lost=0. |
| 2026-06-20 | rc_test V7.10 | 4197350 | claude/wifi-latency-54 | UNO R4 WiFi / COM7 | Dashboard page caching (#54). USB-serial capture confirmed the loop froze 1–2 s on every page load (24 stalls/35 s) because the 33 KB HTML was served Cache-Control: no-store → re-downloaded as ~33×1 KB blocking modem writes each refresh. Fix: serve "/" with ETag + Cache-Control: no-cache, parse If-None-Match, reply 304 on match. **Field-confirmed working** — refreshes no longer freeze the loop; first load still ~2 s once (inherent, full fix = #55 ESP32 offload). SSE path was already numbers-only. 39% flash / 29% RAM. |
| 2026-06-20 | rc_test V7.10 + dash | 4197350 (fw) + a15ecea (page) | combined: wifi-latency-54 fw + dashboard-graphics-59 page | UNO R4 WiFi / COM7 | Combined local build: V7.10 caching firmware (#54) + new dashboard (#59 graphics: bigger battery/RPM, per-motor watts; #61 PWA: apple-mobile-web-app meta + apple-touch-icon + viewport-fit=cover for standalone home-screen launch & landscape fit). 40% flash / 29% RAM. Flashed to verify graphics + add-to-home-screen; caching unaffected (ETag auto-invalidated by page-length change). PRs #57/#60 pending review + a pre-existing markdownlint fix on the handoff doc. |
| 2026-06-20 | rc_test V7.11 | 0b61273 | claude/beeper-horn-63 | UNO R4 WiFi / COM7 | Beeper/horn + dashboard polish. [BEEPER] module on D8 (active piezo): RC SWD/CH7 horn (#63, bench-confirmed via sketches/beeper_test) + "beep beep" on Wi-Fi AP ready (#64). Dashboard: shrink-to-fit any iPhone/iPad screen (#66, position:fixed + visualViewport scale) + smooth all gauges/numbers via per-frame easing (#67). No control-path change. 41% flash / 29% RAM. Field test pending: confirm horn, AP-ready beep, screen fit on small iPad, smooth gauge transitions. |
| 2026-06-20 | rc_test V7.11 (layout fix) | 2003d84 | claude/beeper-horn-63 | UNO R4 WiFi / COM7 | Dashboard fit rework (#66): the position:fixed + translate centering broke on iOS (showed only top-left corner, no telemetry — whole script effectively not fitting/connecting on a stale cached page). Reverted to simple flex-center + transform:scale() (visualViewport size + 200/600 ms retries) and removed user-scalable=no so the operator can pinch-zoom as a fallback. Verified in headless Chrome at iPad Mini + iPad (both orientations). **Operator-confirmed "looks decent" on device** after clearing the iPad's cached page. Current build on chip. 41% flash / 29% RAM. |
| 2026-06-20 | rc_test V7.12 | 604ae84 | agent/C-Builder/battery-inactivity-beeper | UNO R4 WiFi / COM7 | Battery + inactivity alarms on D8 (#51), audio only — no motor-path change. New [ALERT] module layered on the [BEEPER] engine: inactivity = RC off (sbusValid false) > 60 s → one long beep / 2 s (non-latching); low battery = worst-of-two pack EMA < 10.5 V → three fast chirps / ~1.2 s, validity-gated (both ESCs plausible 6–13 V so an unpowered ~0 V pack can't false-alarm), 3 s sag debounce, 60 s startup grace, latched until power cycle. Beep vocabulary distinct from horn/Wi-Fi-ready. OPERATOR-GUIDE.md gains "Beep meanings" table (#70). 41% flash / 29% RAM. Compiled + uploaded clean. **Bench test pending:** RC-off timing, single-pack silence, latch-through-recovery, sag-no-latch, motor output unchanged. Motor cutoff = PR #2 (#65). |
| 2026-06-20 | rc_test V7.12 (test 12.0 V) | 8a33a9b + local edit (uncommitted) | agent/C-Builder/battery-inactivity-beeper | UNO R4 WiFi / COM7 | TEMP bench build: LOWV_THRESH_V 10.5→12.0 (uncommitted) to test the low-V alarm without deep-discharging the packs (resting ~12.2/12.4 V). **PASS** — low-V alarm chirped (three fast / ~1.2 s) when the worse pack reached ~12 V and latched. Production value 10.5 V then restored. |
| 2026-06-20 | rc_test V7.12 (production) | 8a33a9b | agent/C-Builder/battery-inactivity-beeper | UNO R4 WiFi / COM7 | Production 10.5 V build re-flashed after the 12 V test confirmed the alarm mechanics. Matches PR #71 HEAD. 41% flash / 29% RAM. Clean upload. (Note: the 12 V latch test power-cycled the board → dropped off COM7 mid-session; replug fixed it.) **Field-pending:** real low-V behavior under load — see DECISION-LOG fast-EMA finding before relying on 10.5 V live. |
| 2026-06-21 | rc_test V7.13 | 3305cc3 | agent/C-Builder/wifi-loop-watchdog-69 | UNO R4 WiFi / COM7 | P0 runaway failsafe (#69): RA4M1 hardware watchdog (WDT 250 ms) armed end of setup(), WDT.refresh() only after a successful control update (inputs read + outputs written) in loop() — any loop stall past 250 ms resets the MCU → PWM stops → ESCs neutral. Plus incremental Wi-Fi serving: wifiUpdate() does at most ONE modem write/loop pass (one 1 KB page chunk OR one request OR one SSE frame), body streams via pageRemaining state machine, headers/304/data coalesced to single writes — so serving never starves the loop. No motor-mixing change. Also cleared inherited #71 cpplint cosmetics. 41% flash / 29% RAM. Compiled + cpplint clean + uploaded. **Wheels-up bench test PENDING:** (A) throttle + RESET → ESCs stop; (B) hammer dashboard refresh while driving → no runaway, no false WDT reset; confirm "# WDT armed" on serial. |
| 2026-06-21 | rc_test V7.13 (review fixes) | dd5b098 | agent/C-Builder/wifi-loop-watchdog-69 | UNO R4 WiFi / COM7 | Re-flash of V7.13 after Copilot review on PR #74: guard snprintf returns in wifiBeginPage/wifiSend304 (negative/truncation), and abort the incremental page transfer on a 0-byte write (was: spin forever, page never completes, SSE starved). Happy-path behavior unchanged. **Operator-confirmed working:** dashboard refresh no longer stalls/resets the loop (incremental serving holds); the WDT reset is accepted as the last-resort emergency stop only. 42% flash / 29% RAM. |
| 2026-06-21 | rc_test V7.13 (dash UI) | 1631b81 | agent/C-Builder/dashboard-ui-tweaks-75 | UNO R4 WiFi / COM7 | Dashboard UI tweaks (#75), monitoring only — no control change. Power scale right-sized so bars move at real draw: per-motor PWR_MAX 750→300 W, total WATTS_MAX 1500→450 W. Bottom bars thicker (6→14 px) + more spacing (gap 10→18 px). Mode pills (RC/JOYSTICK/DUAL MIX) enlarged (font 0.5→0.95 em). web_page.h + dashboard/index.html synced; ETag auto-invalidates on length change. 42% flash / 29% RAM. Flashed for live visual check; radial dial arc thickness left as-is pending operator eyeball. |
| 2026-06-21 | rc_test V7.13 (dash arrow) | 42bdffc | agent/C-Builder/dashboard-ui-tweaks-75 | UNO R4 WiFi / COM7 | Dashboard: smart 2D direction arrow (heading from track PWMs via atan2(turn,fwd); green→orange→red by forward-vs-reverse; CSS-animated; deadzone) + power scale per-motor 200 W / total 400 W. Monitoring only. **Verified LOCALLY with mock data (tools/ui-test.mjs, Playwright)** before flashing — arrow correct for fwd/rev/pivot-L/R/diagonals/deadzone; then flashed. 42% flash / 29% RAM. |
