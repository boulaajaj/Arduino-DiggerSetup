name: Arduino CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  compile:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        sketch:
          - rc_test
          - hw_diagnostic
          - pin_test
          - sbus_test
          - sbus_d12_test
          - telem_decode
          - usb_test
    steps:
      - uses: actions/checkout@v4
      - uses: arduino/setup-arduino-cli@v2
      - name: Install board and libraries
        run: |
          arduino-cli core update-index
          arduino-cli core install arduino:renesas_uno
          arduino-cli lib install "Bolder Flight Systems SBUS"
          arduino-cli lib install Servo
      - name: Create Wi-Fi secrets from template (#125 — placeholders, CI never sees real credentials)
        run: cp sketches/rc_test/arduino_secrets.h.example sketches/rc_test/arduino_secrets.h
      - name: Compile ${{ matrix.sketch }}
        run: arduino-cli compile --fqbn arduino:renesas_uno:unor4wifi sketches/${{ matrix.sketch }}
